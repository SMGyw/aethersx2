name: iOS Build Test
on: [push, pull_request]

jobs:
  build-ios:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install CMake and Dependencies
        run: |
          brew install cmake ninja

      - name: Create Dummy wxWidgets Headers
        run: |
          mkdir -p dummy_wx/wx
          touch dummy_wx/wx/string.h
          touch dummy_wx/wx/log.h
          touch dummy_wx/wx/thread.h
          touch dummy_wx/wx/gdicmn.h
          touch dummy_wx/wx/msgdlg.h
          touch dummy_wx/wx/app.h
          touch dummy_wx/wx/confbase.h
          touch dummy_wx/wx/fileconf.h
          touch dummy_wx/wx/event.h

      - name: Configure CMake
        run: |
          rm -rf build
          mkdir -p build
          export IOS_SDK_PATH=$(xcrun --sdk iphoneos --show-sdk-path)
          echo "Current iOS SDK Path: $IOS_SDK_PATH"
          
          cmake -S . -B build \
            -G "Unix Makefiles" \
            -DCMAKE_SYSTEM_NAME=iOS \
            -DCMAKE_OSX_SYSROOT=$IOS_SDK_PATH \
            -DCMAKE_OSX_ARCHITECTURES=arm64 \
            -DCMAKE_BUILD_TYPE=Release \
            -DUSE_WXWIDGETS=OFF \
            -DPCSX2_CORE=ON \
            -DENABLE_SETCAP=OFF

      - name: Build Core
        run: |
          # 1. Environment Prep
          rm -rf build && mkdir build
          mkdir -p dummy_wx/wx
          export LC_CTYPE=C
          export LANG=C
          
          # 2. SOURCE SURGERY (The 31% & 43% Permanent Fix)
          # We disable PCH and macOS-specific files directly in the source logic
          echo "Disabling PCH and AGL in CMakeLists..."
          find . -name "CMakeLists.txt" -exec sed -i '' 's/target_precompiled_headers/#/g' {} +
          sed -i '' '/ContextAGL.mm/d' common/CMakeLists.txt
          
          # 3. Neutralize macOS/Audio headers
          echo "int dummy_agl = 0;" > common/GL/ContextAGL.mm
          echo "#pragma once" > common/GL/ContextAGL.h
          echo "void dummy_res() {}" > 3rdparty/cubeb/src/cubeb_resampler.cpp

          # 4. Refined Mock Bridge Header
          cat <<EOF > dummy_wx/wx_bridge.h
          #pragma once
          #include <string>
          #include <stdarg.h>
          #include <algorithm>
          class wxString : public std::string {
          public:
              using std::string::string;
              wxString() : std::string("") {}
              wxString(const std::string& s) : std::string(s) {}
              wxString(const char* s) : std::string(s ? s : "") {}
              const char* c_str() const { return std::string::c_str(); }
              bool IsEmpty() const { return empty(); }
              void Clear() { clear(); }
              wxString Lower() const { wxString s = *this; std::transform(s.begin(), s.end(), s.begin(), ::tolower); return s; }
              bool StartsWith(const std::string& prefix) const { return this->compare(0, prefix.length(), prefix) == 0; }
              static wxString Format(const char* fmt, ...) {
                  char buf[4096]; va_list args; va_start(args, fmt);
                  vsnprintf(buf, sizeof(buf), fmt, args); va_end(args);
                  return wxString(buf);
              }
          };
          #define wxLogMessage(...) printf(__VA_ARGS__)
          #define wxPrintf(...) printf(__VA_ARGS__)
          #define wxT(x) x
          #define _(x) x
          EOF

          cd build
          
          # 5. Configure with hard-coded bypasses
          cmake .. -DCMAKE_BUILD_TYPE=Release \
                   -DUSE_CUBEB=OFF -DBUILD_CUBEB=OFF \
                   -DUSE_WXWIDGETS=OFF -DUSE_OPENGL=OFF \
                   -DCMAKE_C_FLAGS="-DHAVE_FDOPEN=0 -Dfdopen=z_ios_fdopen" \
                   -DCMAKE_CXX_FLAGS="-DHAVE_FDOPEN=0 -Dfdopen=z_ios_fdopen" \
                   -DPCSX2_USE_PCH=OFF -DCLANG_ENABLE_PCH=OFF -DENABLE_PCH=OFF \
                   -DCMAKE_DISABLE_PRECOMPILED_HEADERS=ON

          # 6. Targeted Injection for Core Modules
          for mod in "common" "core" "pcsx2"; do
            if [ -d "$mod/CMakeFiles" ]; then
              find "$mod/CMakeFiles" -name "flags.make" -exec sed -i '' "s|CXX_FLAGS = |CXX_FLAGS = -include ${GITHUB_WORKSPACE}/dummy_wx/wx_bridge.h |" {} +
            fi
          done

          # 7. Final Clean for generated artifacts
          find . -name "flags.make" -exec sed -i '' 's/-include-pch [^ ]*//g' {} +
          find . -name "flags.make" -exec sed -i '' 's/-mfpmath=sse//g' {} +
          find . -name "flags.make" -exec sed -i '' 's/-x objective-c++/ /g' {} +

          # 8. Start Build
          cmake --build . --config Release -- -j $(sysctl -n hw.logicalcpu)